{
  "name": "@qavio/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared domain types for Qavio, used by the control plane, queue, and execution plane.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@qavio/config": "workspace:*",
    "@qavio/eslint-config": "workspace:*",
    "@qavio/testing": "workspace:*",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
