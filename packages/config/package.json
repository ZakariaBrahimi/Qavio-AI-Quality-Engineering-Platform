{
  "name": "@qavio/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript configuration and environment-validation helpers for Qavio.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "tsconfig",
    "src"
  ],
  "scripts": {
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@qavio/eslint-config": "workspace:*",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
