{
  "name": "@qavio/testing",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Vitest configuration and test utilities for Qavio packages.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint . --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "vitest": "^2.1.1"
  },
  "devDependencies": {
    "@qavio/config": "workspace:*",
    "@qavio/eslint-config": "workspace:*",
    "typescript": "^5.5.4"
  }
}
