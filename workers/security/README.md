# @qavio/worker-security

Coming soon. This worker will run automated security QA scans (e.g.
dependency and basic vulnerability checks) against a project's
environments.

Not implemented in Phase 1 — see `docs/architecture.md` for scope.
