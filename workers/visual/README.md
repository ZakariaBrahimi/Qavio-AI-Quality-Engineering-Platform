# @qavio/worker-visual

Coming soon. This worker will run visual regression checks (baseline
screenshot capture and diffing) against a project's environments.

Not implemented in Phase 1 — see `docs/architecture.md` for scope.
